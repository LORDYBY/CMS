# events {}

# http {
#     sendfile on;
#     tcp_nopush on;
#     aio threads;

#     server {
#         listen 80;
#         server_name localhost;

#         # MAIN MEDIA DIRECTORY
#         location /media/ {
#             alias /media/;
#             autoindex off;
#             add_header Cache-Control "public, max-age=3600";
#             add_header Accept-Ranges bytes;
#             try_files $uri =404;
#         }
#  }
# }


############################################

# user nginx;
# worker_processes auto;

# events {
#     worker_connections 4096;
# }

# http {
#     include       mime.types;
#     default_type  application/octet-stream;

#     sendfile        on;
#     tcp_nopush      on;
#     tcp_nodelay     on;
#     keepalive_timeout 65;

#     # LOGS
#     access_log /var/log/nginx/access.log;
#     error_log  /var/log/nginx/error.log warn;

#     # SECURITY HEADERS
#     add_header X-Content-Type-Options nosniff;
#     add_header X-Frame-Options DENY;
#     add_header X-XSS-Protection "1; mode=block";
#     add_header Referrer-Policy no-referrer;
#     add_header Permissions-Policy "fullscreen=(self)";

#     server_tokens off;

#     # =========================================================
#     # STATIC MEDIA SERVER (FIXED!)
#     # =========================================================
#     server {
#         listen 80;
#         server_name _;

#         #
#         # *** MAIN MEDIA DIRECTORY ***
#         #
#         location /media/ {
#             alias /media/;
#             autoindex off;

#             # Streaming support
#             add_header Accept-Ranges bytes;

#             # Cache
#             add_header Cache-Control "public, max-age=3600";

#             try_files $uri =404;
#         }

#         #
#         # *** NO REGEX VIDEO LOCATIONS ***  
#         # Because they BREAK alias path resolution
#         #

#         # Block hidden files
#         location ~ /\. {
#             deny all;
#         }
#     }
# }

###############################################

user nginx;
worker_processes auto;

events {
    worker_connections 4096;
}

http {
    include mime.types;
    default_type application/octet-stream;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log warn;

    add_header X-Content-Type-Options nosniff;
    add_header X-Frame-Options DENY;
    add_header X-XSS-Protection "1; mode=block";
    add_header Referrer-Policy no-referrer;
    add_header Permissions-Policy "fullscreen=(self)";

    server_tokens off;

    server {
        listen 80;
        server_name _;

        # ---------------------------
        # API → Reverse proxy to FastAPI
        # ---------------------------
        location /api/ {
            proxy_pass http://host.docker.internal:8000;

            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # ---------------------------
        # Static media
        # ---------------------------
        location /media/ {
            alias /media/;
            autoindex off;
            add_header Accept-Ranges bytes;
            add_header Cache-Control "public, max-age=3600";
            try_files $uri =404;
        }

        # ---------------------------
        # Player files (PWA)
        # ---------------------------
        location /player/ {
            alias /player/;
            try_files $uri $uri/ =404;
        }

        location ~ /\. {
            deny all;
        }
    }
}



